const factory = require('./04-angular-events');

describe('04 - angular events', () => {
  it('should listen for eventName', () => {

  });

  it('should broadcast otherEventName', () => {

  });

  it('should emit otherOtherEventName on root', () => {

  });
});
